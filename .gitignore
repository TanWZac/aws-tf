.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
terraform.tfvars
*.auto.tfvars
*.auto.tfvars.json

# Local plan artifacts
*.plan
*.tfplan

# Editor/OS
.DS_Store
.vscode/

# Local secrets
env.sh
.env

# Load-test outputs
# environments/*/backend.hcl is COMMITTED: bucket/table names are not secrets and
# the backend should be reviewable. Nested per-app backends stay local.
environments/*/*/backend.hcl
loadtest/results/

# Graphify — commit graphify-out/ for team knowledge graph, exclude local-only file
graphify-out/cost.json

# Plan-scoped worktrees (superpowers subagent-driven-development)
.worktrees/
